{% for related_path, score in related_pages %}
        {% set related = grav['pages'].get(related_path) %}
        {% if related %}
        <aside class="pure-u-1 pure-u-md-1-2 pure-u-lg-1-3 pure-u-xl-1-4 card item">
            <div class="edge item-load list-blog-header">
                 <div class="list-blog-top">
                    <h3 class="blog-item-title"><a href="{{ related.url }}" title="{{ related.title }}">{{ related.title }}</a></h3>
                   <span class="list-blog-date">{{ page.date|date(config.system.pages.dateformat.short) }}</span>
                </div>
                {% set header_image_media = related.media.images|first %}
                {{ header_image_media.cropZoom(600, 300).html }}
                {{ related.content|truncate(300) }}
               <p><a href="{{ related.url }}">[click here to read more]</a></p>
            </div>
        </aside>
        {% endif %}
{% endfor %}
